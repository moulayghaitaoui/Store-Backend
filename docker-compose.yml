version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: storeuser
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: storedb
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

volumes:
  db-data:
